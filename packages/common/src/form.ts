// this will be gone once we rework the Active Request page, databases is working on it

export type Form = {
  receiverName: string;
  message: string;
  senderName: string;
  flowerType: string;
  roomNumber: number;
};
